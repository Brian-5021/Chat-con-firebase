// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
    firebaseConfig : {
    // tslint:disable-next-line: quotemark
    apiKey: "AIzaSyBIr2pF7W4A-sRFveJTsa9iZQ6wuGgw3BY",
    // tslint:disable-next-line: quotemark
    authDomain: "firechat-4778b.firebaseapp.com",
    // tslint:disable-next-line: quotemark
    databaseURL: "https://firechat-4778b.firebaseio.com",
    // tslint:disable-next-line: quotemark
    projectId: "firechat-4778b",
    // tslint:disable-next-line: quotemark
    storageBucket: "firechat-4778b.appspot.com",
    // tslint:disable-next-line: quotemark
    messagingSenderId: "960326070575",
    // tslint:disable-next-line: quotemark
    appId: "1:960326070575:web:c33fe6e52a98fd758271da"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
